# Next.js
.next/
out/
build/

# Dependencies
node_modules/

# Python generated files
__pycache__/
*.py[cod]

# Environment — never commit real credentials
.env
.env.local
.env*.local
credentials/pdi.env
credentials/gcp-service-account.json
credentials/pdi-credentials.json

# Credentials — GCP service account keys
*.json
!package.json
!package-lock.json
!tsconfig.json
!credentials/*.example
!credentials/*.example.json

# PDI folders — JSON/CSV contents gitignored via *.json above; .gitkeep is tracked
# pdi-mappings/   Mapper + Syncer mapping JSON
# pdi-sync-exports/   Sync CSV reports

# Legacy path (migrated into pdi-mappings on first list)
data/pdi-mappings/

# Local BQ snapshot cache (regenerated on server)
data/bq-snapshots/

# District classifier runtime data — may contain uploaded voter/contact CSVs
data/district-classifier/
data/district-sort-cache/

# Sync export artifacts
pdi-sync-exports/*
!pdi-sync-exports/.gitkeep

# Logs
*.log

# OS
.DS_Store
Thumbs.db
