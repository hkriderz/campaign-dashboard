import MapperWorkspace from "./MapperWorkspace";

export const metadata = {
  title: "PDI Mapper — Campaign Dashboard",
  description: "Map Scale to Win survey answers to PDI flags.",
};

export default function PdiMapperPage() {
  return <MapperWorkspace />;
}
